version: 2
jobs:
  build:
    branch: /feature_.*/
    docker:
      - image: circleci/node
    steps:
      - checkout
      - run: echo "hello feature"
  build2:
    branch: dev_integration
    docker:
      - image: circleci/node
    steps:
      - checkout
      - run: echo "hello integration"