version: 2
jobs:
  build:
    docker:
      - image: circleci/node
    branch: /feature_.*/
    steps:
      - checkout
      - run: 
          command: echo "hello $CIRCLE_BRANCH"
          command: git merge dev_integration
  build:
    docker:
      - image: circleci/node
    branch: dev_integration
    steps:
      - checkout
      - run: 
          command: echo "hello $CIRCLE_BRANCH"