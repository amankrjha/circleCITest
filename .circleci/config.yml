version: 2
jobs:
  build:
    docker:
      - image: circleci/node
    branch: /feature_.*/
    steps:
      - checkout
      - run: 
          commands: 
            - echo "hello $CIRCLE_BRANCH"
            - git merge dev_integration
            - echo "git merge done"
  build:
    docker:
      - image: circleci/node
    branch: dev_integration
    steps:
      - checkout
      - run: 
          command: echo "hello $CIRCLE_BRANCH"