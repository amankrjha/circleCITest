version: 2
jobs:
  build:
    docker:
      - image: circleci/node
    steps:
      - checkout
      -> if [$CIRCLE_BRANCH == "feature_.*"]; then 
        echo "Hello feature";
        git merge dev_integration;
        fi