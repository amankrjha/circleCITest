version: 2
jobs:
  checkout:
    post:
      - git merge dev_integration
  build:
    docker:
      - image: circleci/node
    steps:
      - checkout
      - run echo "hello $CIRCLE_BRANCH"