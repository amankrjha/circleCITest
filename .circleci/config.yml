version: 2
jobs:
  feature_build:
    docker:
      - image: circleci/node
    branches: 
      only: 
        - "feature_.*"
    steps:
      - checkout
      - run: 
        command: echo "hello $CIRCLE_BRANCH"
        command: git merge dev_integration